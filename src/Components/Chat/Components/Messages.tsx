import React from "react";
import {MessagesProps} from "../ChatTypes";

export default function Messages(props: MessagesProps) {
    return (
        <div className='col-span-3 border-r border-l'>messages</div>
    );
}